package net.frostytrix.fletcherstrestle.client.model;

// TODO(port-26.1): RegisterGeometryLoaders is gone in 26.1. Custom
// item models now register via a different mechanism (ItemModels?).
// Stubbed so the rest of the client code compiles.
public final class ModularModelLoader {
    public static final ModularModelLoader INSTANCE = new ModularModelLoader();
    private ModularModelLoader() {}
}
