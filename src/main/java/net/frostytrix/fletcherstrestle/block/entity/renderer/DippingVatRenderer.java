package net.frostytrix.fletcherstrestle.block.entity.renderer;

// TODO(port-26.1): BlockEntityRenderer<T, S extends BlockEntityRenderState>
// now uses the extract/submit pattern. Stubbed for compile; the fluid
// height + colour layer it used to draw will render as default vanilla.
public final class DippingVatRenderer {
    private DippingVatRenderer() {}
}
