# AbstractArrow moved to .arrow subpackage in 26.1.

# Expose inGround for EagleFetchGoal — the only authoritative "stuck" signal.
public net.minecraft.world.entity.projectile.arrow.AbstractArrow inGround

# Expose setPierceLevel for dark_oak shaft piercing — vanilla made it
# private; piercing is normally weapon-enchant driven, we drive it from
# the shaft material.
public net.minecraft.world.entity.projectile.arrow.AbstractArrow setPierceLevel(B)V

# Expose baseDamage field — getBaseDamage() was removed in 26.1, but we
# need to read it for our distance/executioner/backstab damage scaling.
public net.minecraft.world.entity.projectile.arrow.AbstractArrow baseDamage
