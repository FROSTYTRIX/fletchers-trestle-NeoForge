# 26.1 — AbstractArrow moved to .arrow subpackage.
# inGround field was removed (use isInGround() method instead).
# hasImpulse field was removed.

# setPierceLevel — vanilla made it private; piercing is normally
# weapon-enchant driven, we drive it from the shaft material (dark_oak).
public net.minecraft.world.entity.projectile.arrow.AbstractArrow setPierceLevel(B)V

# Expose baseDamage field — getBaseDamage() was removed in 26.1.
public net.minecraft.world.entity.projectile.arrow.AbstractArrow baseDamage

# Expose isInGround() so EagleFetchGoal can detect stuck arrows.
public net.minecraft.world.entity.projectile.arrow.AbstractArrow isInGround()Z
